// =========================================================
// AURA AI 1.3 CONTROLLER
// Cosmic Silk Fluid Background, Morphing Input Capsule, 
// Iridescent Avatars, Streaming Engine & Live Voice Sync
// =========================================================

const _0xinit = () => {
  const _0x4f = "RlVDSyBZT1UgOikgQWNjZXNzIERlbmllZA==";
  void _0x4f;

  const _0xd = [80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,249,218,184,249,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,249,250,184,249,236,184,250,193,184,250,193,184,250,193,184,250,193,184,250,237,184,249,236,184,249,254,184,249,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,249,234,184,250,197,184,250,219,184,250,210,184,248,233,184,251,218,152,244,184,248,218,184,249,250,184,249,238,184,251,229,184,250,229,184,250,193,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,218,184,251,196,184,250,219,184,250,218,184,250,218,184,250,218,184,250,218,184,249,229,184,249,229,184,249,229,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,218,184,251,228,184,250,218,184,248,218,184,249,250,184,249,254,184,249,222,184,248,226,184,249,229,184,249,229,184,249,229,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,249,228,184,250,219,184,250,218,184,249,228,184,250,209,184,250,210,184,250,227,184,249,229,184,249,229,184,249,229,184,249,229,184,251,221,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,226,184,251,213,122,184,250,218,184,248,225,184,251,220,184,250,218,184,250,218,184,248,227,184,249,229,184,249,229,184,249,229,184,251,221,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,226,184,251,221,122,31,122,184,248,226,184,251,221,184,250,218,184,250,218,184,250,210,184,249,229,184,249,229,184,249,229,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,226,184,251,221,122,8,122,184,248,226,184,249,221,184,250,218,184,250,218,184,250,218,184,249,229,184,249,229,184,251,229,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,226,184,251,221,122,106,122,184,248,226,184,249,229,184,250,218,184,250,218,184,248,250,184,249,229,184,249,229,184,251,221,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,226,184,249,237,184,250,218,8,184,250,210,184,249,229,184,250,218,184,248,218,184,249,228,184,249,229,184,249,229,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,249,229,184,251,218,184,250,218,184,250,218,184,250,225,184,248,236,184,249,229,184,249,229,184,249,229,184,251,221,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,218,184,249,254,184,251,228,184,250,217,184,250,218,184,250,218,184,250,218,184,250,210,184,249,229,184,249,229,184,249,229,184,250,219,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,248,250,184,249,236,184,249,245,184,249,254,184,249,254,184,249,254,184,249,254,184,251,238,184,250,236,184,250,236,184,249,230,184,249,229,184,249,229,184,249,237,184,249,254,184,249,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,80,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,218,184,250,211,184,250,211,184,250,211,184,250,219,184,250,218,184,250,218,80,122,122,80,122,122,122,122,122,122,122,10,122,27,122,20,122,29,122,21,122,9,122,21,122,8,122,21,122,9,80,122,122];
  const _0x9b = new TextDecoder().decode(new Uint8Array(_0xd.map(b => b ^ 0x5a)));
  const _0x1c = 'color:#55ff00;font-family:monospace;font-size:13px;text-shadow:0 0 8px rgba(85,255,0,0.6);';
  console.log(`%c${_0x9b}`, _0x1c);
};
_0xinit();

document.addEventListener('DOMContentLoaded', () => {
  // Elements
  const messagesContainer = document.getElementById('messagesContainer');
  const messageInput = document.getElementById('messageInput');
  const inputCapsule = document.getElementById('inputCapsule');
  const sendBtn = document.getElementById('sendBtn');
  const welcomeHero = document.getElementById('welcomeHero');
  const newChatBtn = document.getElementById('newChatBtn');
  const newChatFullBtn = document.getElementById('newChatFullBtn');
  const quickNewBtn = document.getElementById('quickNewBtn');
  const historyList = document.getElementById('historyList');
  const clearChatBtn = document.getElementById('clearChatBtn');
  const modelSelectorPill = document.getElementById('modelSelectorPill');
  const sidebarToggleBtn = document.getElementById('sidebarToggleBtn') || document.getElementById('mobileMenuBtn');
  const sidebar = document.getElementById('sidebar');

  // Settings elements
  const openSettingsBtn = document.getElementById('openSettingsBtn');
  const closeSettingsBtn = document.getElementById('closeSettingsBtn');
  const settingsModal = document.getElementById('settingsModal');
  const saveSettingsBtn = document.getElementById('saveSettingsBtn');
  const apiProviderSelect = document.getElementById('apiProviderSelect');
  const apiKeyGroup = document.getElementById('apiKeyGroup');
  const apiKeyInput = document.getElementById('apiKeyInput');
  const speechSpeed = document.getElementById('speechSpeed');
  const speedDisplay = document.getElementById('speedDisplay');

  // Model Credit & Quota System State
  const DEFAULT_MODEL_QUOTAS = {
    'gemini': {
      name: 'Google Gemini 1.5 Flash',
      provider: 'Google AI',
      maxRequests: 1500,
      maxTokens: 1000000,
      usedRequests: 0,
      usedTokens: 0,
      type: 'Cloud (Daily Free Tier: 1500 RPD)'
    },
    'openai': {
      name: 'OpenAI GPT-4o Mini',
      provider: 'OpenAI',
      maxRequests: 500,
      maxTokens: 250000,
      usedRequests: 0,
      usedTokens: 0,
      type: 'Cloud Pay-As-You-Go ($5.00 Base)'
    },
    'ollama': {
      name: 'Ollama (qwen3:4b-instruct)',
      provider: 'Local Hardware',
      maxRequests: 999999,
      maxTokens: 999999999,
      usedRequests: 0,
      usedTokens: 0,
      type: 'Local Device (Unlimited Free)'
    },
    'builtin': {
      name: 'SUNO Neural Core',
      provider: 'Built-in Browser/Server',
      maxRequests: 999999,
      maxTokens: 999999999,
      usedRequests: 0,
      usedTokens: 0,
      type: 'Local Hybrid (Unlimited)'
    }
  };

  let modelCredits = JSON.parse(localStorage.getItem('aura_model_credits') || 'null');
  if (!modelCredits) {
    modelCredits = DEFAULT_MODEL_QUOTAS;
    localStorage.setItem('aura_model_credits', JSON.stringify(modelCredits));
  } else {
    // Ensure all model keys exist
    for (const k in DEFAULT_MODEL_QUOTAS) {
      if (!modelCredits[k]) {
        modelCredits[k] = DEFAULT_MODEL_QUOTAS[k];
      }
    }
  }

  let currentSessionId = Date.now().toString();
  let chatSessions = JSON.parse(localStorage.getItem('aura_sessions') || '{}');
  let activeMessages = [];
  let isGenerating = false;
  let speechSynth = window.speechSynthesis;
  let settings = JSON.parse(localStorage.getItem('aura_settings') || JSON.stringify({
    provider: 'gemini',
    apiKey: '',
    speed: 1.05
  }));

  function trackModelUsage(providerOrModel, userText = '', assistantText = '') {
    let key = 'gemini';
    const p = (providerOrModel || settings.provider || '').toLowerCase();
    if (p.includes('gpt') || p.includes('openai')) key = 'openai';
    else if (p.includes('ollama') || p.includes('qwen')) key = 'ollama';
    else if (p.includes('builtin') || p.includes('offline') || p.includes('neural')) key = 'builtin';
    else key = 'gemini';

    if (!modelCredits[key]) {
      modelCredits[key] = Object.assign({}, DEFAULT_MODEL_QUOTAS[key] || DEFAULT_MODEL_QUOTAS['gemini']);
    }

    // Estimate realistic token usage: ~1 token per 4 chars of user prompt + assistant output
    const promptTokens = Math.max(1, Math.ceil((userText || '').length / 4));
    const outputTokens = Math.max(1, Math.ceil((assistantText || '').length / 4));
    const totalTurnTokens = promptTokens + outputTokens;

    modelCredits[key].usedRequests = (modelCredits[key].usedRequests || 0) + 1;
    modelCredits[key].usedTokens = (modelCredits[key].usedTokens || 0) + totalTurnTokens;

    localStorage.setItem('aura_model_credits', JSON.stringify(modelCredits));
    renderCreditMonitor();
  }

  window.trackModelUsage = trackModelUsage;

  function renderCreditMonitor() {
    const list = document.getElementById('modelCreditList');
    if (!list) return;

    list.innerHTML = '';
    for (const [key, data] of Object.entries(modelCredits)) {
      const isUnlimited = data.maxRequests >= 900000;
      const remainingReq = isUnlimited ? 'Unlimited' : Math.max(0, data.maxRequests - data.usedRequests);
      const remainingTok = isUnlimited ? 'Unlimited' : Math.max(0, data.maxTokens - data.usedTokens);
      
      const percentUsed = isUnlimited ? 0 : Math.min(100, Math.round((data.usedRequests / data.maxRequests) * 100));
      const percentRemaining = isUnlimited ? 100 : (100 - percentUsed);

      const item = document.createElement('div');
      item.className = 'model-credit-item';
      item.innerHTML = `
        <div class="model-credit-top">
          <span class="model-name-tag">
            <span class="model-status-dot"></span>
            ${data.name}
          </span>
          <span class="model-tokens-badge">${(data.usedTokens || 0).toLocaleString()} tokens used</span>
        </div>
        <div class="model-credit-progress-bg">
          <div class="model-credit-progress-bar" style="width: ${percentRemaining}%;"></div>
        </div>
        <div class="model-credit-footer-row">
          <span>${data.type}</span>
          <span class="model-percent-text">${isUnlimited ? '100% (Infinite)' : `${remainingReq} requests left (${percentRemaining}%)`}</span>
        </div>
      `;
      list.appendChild(item);
    }
  }

  // Create chat thread container inside messagesContainer
  let chatThread = document.createElement('div');
  chatThread.className = 'aura-chat-thread';
  messagesContainer.appendChild(chatThread);

  // Setup marked options for code syntax highlighting
  marked.setOptions({
    highlight: function(code, lang) {
      if (lang && hljs.getLanguage(lang)) {
        return hljs.highlight(code, { language: lang }).value;
      }
      return hljs.highlightAuto(code).value;
    },
    breaks: true
  });

  // =========================================================
  // 1. COSMIC SILK FLUID BACKGROUND CANVAS ANIMATION
  // Recreating the glowing violet/magenta organic motion from video
  // =========================================================
  const silkCanvas = document.getElementById('cosmicSilkCanvas');
  if (silkCanvas) {
    const sCtx = silkCanvas.getContext('2d');
    let width = silkCanvas.width = window.innerWidth;
    let height = silkCanvas.height = window.innerHeight;

    window.addEventListener('resize', () => {
      width = silkCanvas.width = window.innerWidth;
      height = silkCanvas.height = window.innerHeight;
    });

    let phase = 0;
    function renderCosmicSilk() {
      phase += 0.004;
      sCtx.fillStyle = '#141c15';
      sCtx.fillRect(0, 0, width, height);

      // Render 3 soft, organic, silky ambient waves in moss, sage & cream tones
      const ribbons = [
        { color1: 'rgba(161, 188, 152, 0.20)', color2: 'rgba(20, 28, 21, 0)', yOffset: height * 0.46, freq: 0.0014, speed: 0.7 },
        { color1: 'rgba(119, 136, 115, 0.24)', color2: 'rgba(20, 28, 21, 0)', yOffset: height * 0.54, freq: 0.0018, speed: 0.9 },
        { color1: 'rgba(220, 207, 192, 0.16)', color2: 'rgba(20, 28, 21, 0)', yOffset: height * 0.62, freq: 0.0012, speed: 0.5 }
      ];

      ribbons.forEach((rib, idx) => {
        sCtx.save();
        sCtx.beginPath();
        sCtx.moveTo(0, height);

        for (let x = 0; x <= width; x += 15) {
          const wave1 = Math.sin(x * rib.freq + phase * rib.speed + idx) * 75;
          const wave2 = Math.cos(x * 0.0007 - phase * 0.4) * 45;
          const y = rib.yOffset + wave1 + wave2;
          sCtx.lineTo(x, y);
        }

        sCtx.lineTo(width, height);
        sCtx.closePath();

        const grad = sCtx.createLinearGradient(0, rib.yOffset - 100, width * 0.75, height);
        grad.addColorStop(0, rib.color1);
        grad.addColorStop(0.6, 'rgba(36, 47, 69, 0.12)');
        grad.addColorStop(1, rib.color2);

        sCtx.fillStyle = grad;
        sCtx.filter = 'blur(42px)';
        sCtx.fill();
        sCtx.restore();
      });

      requestAnimationFrame(renderCosmicSilk);
    }
    requestAnimationFrame(renderCosmicSilk);
  }

  // =========================================================
  // 2. INPUT CAPSULE MORPHING & SEND LOGIC
  // =========================================================
  function updateSendBtnState() {
    const hasText = messageInput.value.trim().length > 0;
    sendBtn.disabled = !hasText || isGenerating;
  }

  messageInput.addEventListener('input', () => {
    messageInput.style.height = 'auto';
    messageInput.style.height = Math.min(messageInput.scrollHeight, 160) + 'px';
    updateSendBtnState();
  });

  messageInput.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSendMessage();
    }
  });

  sendBtn.addEventListener('click', handleSendMessage);

  // File Upload / Attachment handler (ChatGPT / Gemini feature)
  const attachBtn = document.getElementById('attachBtn');
  const fileAttachmentInput = document.getElementById('fileAttachmentInput');

  if (attachBtn && fileAttachmentInput) {
    attachBtn.addEventListener('click', () => fileAttachmentInput.click());
    fileAttachmentInput.addEventListener('change', (e) => {
      const file = e.target.files[0];
      if (!file) return;

      const reader = new FileReader();
      reader.onload = (event) => {
        const content = event.target.result;
        messageInput.value = `[Attached File: ${file.name} (${Math.round(file.size / 1024)} KB)]\n${content.substring(0, 4000)}\n\nPlease analyze this: `;
        messageInput.style.height = 'auto';
        messageInput.style.height = Math.min(messageInput.scrollHeight, 160) + 'px';
        updateSendBtnState();
        messageInput.focus();
      };
      if (file.type.startsWith('image/')) {
        messageInput.value = `[Uploaded image: ${file.name}]\nPlease inspect and analyze this image.`;
        updateSendBtnState();
      } else {
        reader.readAsText(file);
      }
      fileAttachmentInput.value = '';
    });
  }

  // Quick Prompt Cards click handler (Fills input box so user can review and send when ready)
  document.querySelectorAll('.aura-prompt-card').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.stopPropagation();
      const prompt = btn.getAttribute('data-prompt');
      if (prompt) {
        messageInput.value = prompt;
        messageInput.style.height = 'auto';
        messageInput.style.height = Math.min(messageInput.scrollHeight, 160) + 'px';
        updateSendBtnState();
        messageInput.focus();
      }
    });
  });

  // Render Single Message Bubble (Screen 6 Reference)
  function renderMessage(role, content) {
    if (welcomeHero) welcomeHero.style.display = 'none';

    const row = document.createElement('div');
    row.className = `aura-message-row ${role}`;

    if (role === 'user') {
      const bubble = document.createElement('div');
      bubble.className = 'aura-user-bubble';
      bubble.textContent = content;
      row.appendChild(bubble);
    } else {
      // Mini Iridescent Sphere Avatar for AI with Logo
      const avatar = document.createElement('div');
      avatar.className = 'aura-mini-sphere-avatar';
      avatar.innerHTML = `<img src="SUNO.png" alt="SUNO AI" class="avatar-logo-mini">`;

      const contentDiv = document.createElement('div');
      contentDiv.className = 'aura-assistant-content';
      contentDiv.innerHTML = marked.parse(content || '');

      row.appendChild(avatar);
      row.appendChild(contentDiv);
      attachCodeCopyButtons(contentDiv);
      attachMessageActions(row, content || '');
    }

    chatThread.appendChild(row);
    messagesContainer.scrollTop = messagesContainer.scrollHeight;
  }

  function attachCodeCopyButtons(container) {
    const pres = container.querySelectorAll('pre');
    pres.forEach(pre => {
      if (pre.querySelector('.aura-code-header')) return;
      const code = pre.querySelector('code');
      const langMatch = (code ? code.className : '').match(/language-(\w+)/);
      const lang = langMatch ? langMatch[1] : 'code';

      const header = document.createElement('div');
      header.className = 'aura-code-header';
      header.innerHTML = `
        <span>${lang}</span>
        <button class="aura-code-copy-btn">
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
            <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
          </svg>
          <span>Copy</span>
        </button>
      `;

      const copyBtn = header.querySelector('.aura-code-copy-btn');
      copyBtn.addEventListener('click', () => {
        const textToCopy = code ? code.innerText : pre.innerText;
        navigator.clipboard.writeText(textToCopy);
        copyBtn.querySelector('span').textContent = 'Copied!';
        setTimeout(() => copyBtn.querySelector('span').textContent = 'Copy', 2000);
      });

      pre.insertBefore(header, pre.firstChild);
    });
  }

  function attachMessageActions(row, text) {
    if (row.querySelector('.aura-message-actions-bar')) return;
    const actionsBar = document.createElement('div');
    actionsBar.className = 'aura-message-actions-bar';

    // Copy action
    const copyBtn = document.createElement('button');
    copyBtn.className = 'aura-msg-action-btn';
    copyBtn.innerHTML = `
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
        <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
      </svg>
      <span>Copy</span>
    `;
    copyBtn.addEventListener('click', () => {
      navigator.clipboard.writeText(text);
      copyBtn.querySelector('span').textContent = 'Copied!';
      setTimeout(() => copyBtn.querySelector('span').textContent = 'Copy', 2000);
    });

    // Read Aloud action
    const speakBtn = document.createElement('button');
    speakBtn.className = 'aura-msg-action-btn';
    speakBtn.innerHTML = `
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon>
        <path d="M19.07 4.93a10 10 0 0 1 0 14.14M15.54 8.46a5 5 0 0 1 0 7.07"></path>
      </svg>
      <span>Read</span>
    `;
    speakBtn.addEventListener('click', () => {
      if (speechSynth.speaking) {
        speechSynth.cancel();
        speakBtn.classList.remove('playing');
        speakBtn.querySelector('span').textContent = 'Read';
        return;
      }
      const plainText = text.replace(/[#*`_]/g, '');
      const utterance = new SpeechSynthesisUtterance(plainText);
      utterance.rate = settings.speed || 1.05;
      utterance.pitch = 1.1; // Warm female pitch

      const isBengali = /[\u0980-\u09FF]/.test(plainText);
      const isHindi = /[\u0900-\u097F]/.test(plainText);

      try {
        const voices = window.speechSynthesis.getVoices() || [];
        if (voices.length > 0) {
          if (isBengali) {
            utterance.voice = voices.find(v => (v.lang && v.lang.startsWith('bn')) && /female|mithu|natural/i.test(v.name))
              || voices.find(v => v.lang && v.lang.startsWith('bn'));
            utterance.lang = 'bn-IN';
          } else if (isHindi) {
            utterance.voice = voices.find(v => (v.lang && v.lang.startsWith('hi')) && /female|kalpana|swara|natural/i.test(v.name))
              || voices.find(v => v.lang && v.lang.startsWith('hi') && !/male|hemant|madhur/i.test(v.name))
              || voices.find(v => v.lang && v.lang.startsWith('hi'));
            utterance.lang = 'hi-IN';
          } else {
            utterance.voice = voices.find(v => v.lang && v.lang.startsWith('en') && /natural.*(jenny|aria|ava|emma)|jenny|aria|zira|samantha|victoria|female/i.test(v.name))
              || voices.find(v => v.lang && v.lang.startsWith('en') && !/david|george|mark|male/i.test(v.name));
            utterance.lang = 'en-US';
          }
        }
      } catch (e) {}

      speakBtn.classList.add('playing');
      speakBtn.querySelector('span').textContent = 'Stop';
      utterance.onend = utterance.onerror = () => {
        speakBtn.classList.remove('playing');
        speakBtn.querySelector('span').textContent = 'Read';
      };
      speechSynth.speak(utterance);
    });

    actionsBar.appendChild(copyBtn);
    actionsBar.appendChild(speakBtn);

    const contentDiv = row.querySelector('.aura-assistant-content');
    if (contentDiv) {
      contentDiv.appendChild(actionsBar);
    }
  }

  // Handle Send & SSE Stream
  async function handleSendMessage() {
    const text = messageInput.value.trim();
    if (!text || isGenerating) return;

    messageInput.value = '';
    messageInput.style.height = 'auto';
    updateSendBtnState();

    activeMessages.push({ role: 'user', content: text });
    renderMessage('user', text);

    isGenerating = true;
    updateSendBtnState();

    // Prepare assistant row
    const row = document.createElement('div');
    row.className = 'aura-message-row assistant';
    
    const avatar = document.createElement('div');
    avatar.className = 'aura-mini-sphere-avatar';
    avatar.innerHTML = `<img src="SUNO.png" alt="SUNO AI" class="avatar-logo-mini">`;

    const contentDiv = document.createElement('div');
    contentDiv.className = 'aura-assistant-content';
    contentDiv.innerHTML = '<span style="color:#4B5096;">Thinking...</span>';

    row.appendChild(avatar);
    row.appendChild(contentDiv);
    chatThread.appendChild(row);
    messagesContainer.scrollTop = messagesContainer.scrollHeight;

    let fullReply = '';

    try {
      const response = await fetch('/api/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          messages: activeMessages,
          persona: 'assistant',
          apiKey: settings.apiKey,
          provider: settings.provider
        })
      });

      const reader = response.body.getReader();
      const decoder = new TextDecoder();
      let streamDone = false;

      while (!streamDone) {
        const { done, value } = await reader.read();
        if (done) break;

        const chunk = decoder.decode(value, { stream: true });
        const lines = chunk.split('\n');

        for (const line of lines) {
          if (line.startsWith('data: ')) {
            const dataStr = line.replace('data: ', '').trim();
            if (!dataStr) continue;

            try {
              const parsed = JSON.parse(dataStr);
              if (parsed.type === 'token') {
                fullReply += parsed.content;
                contentDiv.innerHTML = marked.parse(fullReply);
                messagesContainer.scrollTop = messagesContainer.scrollHeight;
              } else if (parsed.type === 'done') {
                streamDone = true;
                if (parsed.model || parsed.provider) {
                  trackModelUsage(parsed.model || settings.provider, lastUserMsgText, fullReply);
                }
              }
            } catch (e) {}
          }
        }
      }

      activeMessages.push({ role: 'assistant', content: fullReply });
      attachCodeCopyButtons(contentDiv);
      attachMessageActions(row, fullReply);
      saveSession();
      renderCreditMonitor();
    } catch (err) {
      contentDiv.innerHTML = `<span style="color:#ef4444;">Unable to connect to Aura neural core. Please verify your settings.</span>`;
    } finally {
      isGenerating = false;
      updateSendBtnState();
    }
  }

  // Session storage management
  function saveSession() {
    if (!activeMessages.length) return;
    const firstMsg = activeMessages.find(m => m.role === 'user');
    const title = firstMsg ? (firstMsg.content.slice(0, 28) + '...') : 'Aura Session';
    chatSessions[currentSessionId] = {
      id: currentSessionId,
      title: title,
      messages: activeMessages,
      timestamp: Date.now()
    };
    localStorage.setItem('aura_sessions', JSON.stringify(chatSessions));
    renderHistory();
  }

  function renderHistory() {
    historyList.innerHTML = '';
    const sorted = Object.values(chatSessions).sort((a, b) => b.timestamp - a.timestamp);
    sorted.forEach(s => {
      const item = document.createElement('div');
      item.className = `history-item ${s.id === currentSessionId ? 'active' : ''}`;
      
      const titleSpan = document.createElement('span');
      titleSpan.className = 'history-item-title';
      titleSpan.textContent = s.title || 'Conversation';
      titleSpan.title = s.title || 'Conversation';

      item.addEventListener('click', (e) => {
        if (!e.target.closest('.history-item-delete-btn')) {
          loadSession(s.id);
          closeMobileSidebar(e);
        }
      });

      const delBtn = document.createElement('button');
      delBtn.className = 'history-item-delete-btn';
      delBtn.title = 'Delete chat';
      delBtn.innerHTML = `
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="3 6 5 6 21 6"></polyline>
          <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
          <line x1="10" y1="11" x2="10" y2="17"></line>
          <line x1="14" y1="11" x2="14" y2="17"></line>
        </svg>
      `;
      delBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        deleteSession(s.id);
      });

      item.appendChild(titleSpan);
      item.appendChild(delBtn);
      historyList.appendChild(item);
    });
  }

  function deleteSession(id) {
    if (!chatSessions[id]) return;
    delete chatSessions[id];
    localStorage.setItem('aura_sessions', JSON.stringify(chatSessions));
    
    if (currentSessionId === id) {
      const remaining = Object.keys(chatSessions);
      if (remaining.length > 0) {
        loadSession(remaining[0]);
      } else {
        startNewChat();
      }
    } else {
      renderHistory();
    }
  }

  function loadSession(id) {
    if (!chatSessions[id]) return;
    currentSessionId = id;
    activeMessages = chatSessions[id].messages || [];
    chatThread.innerHTML = '';
    if (welcomeHero) welcomeHero.style.display = 'none';

    activeMessages.forEach(m => {
      renderMessage(m.role, m.content);
    });
    renderHistory();
  }

  function startNewChat() {
    currentSessionId = Date.now().toString();
    activeMessages = [];
    chatThread.innerHTML = '';
    if (welcomeHero) welcomeHero.style.display = 'flex';
    messageInput.value = '';
    messageInput.style.height = 'auto';
    updateSendBtnState();
    renderHistory();
  }

  if (newChatBtn) newChatBtn.addEventListener('click', startNewChat);
  if (newChatFullBtn) newChatFullBtn.addEventListener('click', startNewChat);

  clearChatBtn.addEventListener('click', () => {
    if (confirm("Purge all Aura memories and chat sessions?")) {
      chatSessions = {};
      localStorage.removeItem('aura_sessions');
      startNewChat();
      settingsModal.classList.remove('active');
    }
  });

  // Settings
  function openSettings() {
    apiProviderSelect.value = settings.provider || 'gemini';
    apiKeyInput.value = settings.apiKey || '';
    speechSpeed.value = settings.speed || 1.05;
    speedDisplay.textContent = `${settings.speed || 1.05}x`;
    renderCreditMonitor();
    settingsModal.classList.add('active');
  }

  const resetCreditsBtn = document.getElementById('resetCreditsBtn');
  if (resetCreditsBtn) {
    resetCreditsBtn.addEventListener('click', () => {
      if (confirm("Reset token and credit counters for all models back to 100%?")) {
        modelCredits = JSON.parse(JSON.stringify(DEFAULT_MODEL_QUOTAS));
        localStorage.setItem('aura_model_credits', JSON.stringify(modelCredits));
        renderCreditMonitor();
      }
    });
  }

  if (openSettingsBtn) openSettingsBtn.addEventListener('click', openSettings);
  if (modelSelectorPill) modelSelectorPill.addEventListener('click', openSettings);

  closeSettingsBtn.addEventListener('click', () => settingsModal.classList.remove('active'));
  saveSettingsBtn.addEventListener('click', () => {
    settings.provider = apiProviderSelect.value;
    settings.apiKey = apiKeyInput.value.trim();
    settings.speed = parseFloat(speechSpeed.value) || 1.05;
    localStorage.setItem('aura_settings', JSON.stringify(settings));
    settingsModal.classList.remove('active');
  });

  speechSpeed.addEventListener('input', () => {
    speedDisplay.textContent = `${speechSpeed.value}x`;
  });

  const sidebarBackdrop = document.getElementById('sidebarBackdrop');

  function toggleSidebar(e) {
    if (e) e.stopPropagation();
    if (window.innerWidth <= 768) {
      const isOpen = sidebar.classList.toggle('mobile-open');
      if (sidebarBackdrop) {
        sidebarBackdrop.classList.toggle('active', isOpen);
      }
      if (sidebarToggleBtn) {
        sidebarToggleBtn.classList.toggle('sidebar-collapsed', isOpen);
      }
    } else {
      const isCollapsed = sidebar.classList.toggle('collapsed');
      if (sidebarToggleBtn) {
        sidebarToggleBtn.classList.toggle('sidebar-collapsed', isCollapsed);
      }
    }
  }

  function closeMobileSidebar(e) {
    if (e) e.stopPropagation();
    sidebar.classList.remove('mobile-open');
    if (sidebarBackdrop) sidebarBackdrop.classList.remove('active');
    if (sidebarToggleBtn) sidebarToggleBtn.classList.remove('sidebar-collapsed');
  }

  if (sidebarToggleBtn) {
    sidebarToggleBtn.addEventListener('click', toggleSidebar);
  }

  const closeSidebarBtn = document.getElementById('closeSidebarBtn');
  if (closeSidebarBtn) {
    closeSidebarBtn.addEventListener('click', closeMobileSidebar);
    closeSidebarBtn.addEventListener('touchstart', closeMobileSidebar, { passive: true });
  }

  if (sidebarBackdrop) {
    sidebarBackdrop.addEventListener('click', closeMobileSidebar);
    sidebarBackdrop.addEventListener('touchstart', closeMobileSidebar, { passive: true });
  }

  // Close mobile sidebar automatically when clicking a chat session or starting a new session
  if (newChatFullBtn) {
    newChatFullBtn.addEventListener('click', closeMobileSidebar);
  }
  if (newChatBtn) {
    newChatBtn.addEventListener('click', closeMobileSidebar);
  }
  if (historyList) {
    historyList.addEventListener('click', (e) => {
      if (e.target.closest('.history-item')) {
        closeMobileSidebar();
      }
    });
  }

  // Close with Escape key
  window.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && sidebar.classList.contains('mobile-open')) {
      closeMobileSidebar();
    }
  });

  // Shortcut key (Ctrl+B) to toggle sidebar
  window.addEventListener('keydown', (e) => {
    if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === 'b') {
      e.preventDefault();
      toggleSidebar();
    }
  });

  // GSAP Smooth Micro-Animations & Entrance Timelines
  function initGsapAnimations() {
    if (typeof gsap === 'undefined') return;

    // 1. Initial UI reveal timeline
    const tl = gsap.timeline({ defaults: { ease: 'power3.out' } });
    
    if (window.innerWidth > 768) {
      tl.from('.aura-sidebar', {
        x: -40,
        opacity: 0,
        duration: 0.7,
        clearProps: 'transform,opacity'
      });
    }
    
    tl.from('.aura-main-panel', {
      scale: 0.98,
      opacity: 0,
      duration: 0.6
    }, window.innerWidth > 768 ? '-=0.4' : '0')
    .from('.aura-hero-emblem', {
      scale: 0.5,
      opacity: 0,
      duration: 0.8,
      ease: 'back.out(1.7)'
    }, '-=0.3')
    .from('.aura-hero-heading, .aura-hero-sub', {
      y: 20,
      opacity: 0,
      stagger: 0.12,
      duration: 0.6
    }, '-=0.5')
    .from('.aura-prompt-card', {
      y: 25,
      opacity: 0,
      stagger: 0.08,
      duration: 0.5,
      ease: 'back.out(1.4)'
    }, '-=0.4')
    .from('.aura-input-capsule', {
      y: 30,
      opacity: 0,
      duration: 0.6,
      ease: 'back.out(1.5)',
      clearProps: 'transform,opacity'
    }, '-=0.3');

    // 2. Interactive hover spring physics for prompt cards
    document.querySelectorAll('.aura-prompt-card').forEach(card => {
      card.addEventListener('mouseenter', () => {
        gsap.to(card, { y: -4, scale: 1.02, duration: 0.25, ease: 'power2.out' });
      });
      card.addEventListener('mouseleave', () => {
        gsap.to(card, { y: 0, scale: 1.0, duration: 0.25, ease: 'power2.out' });
      });
    });

    // 3. Floating pulse for Hero sphere
    gsap.to('.aura-sphere-halo', {
      scale: 1.2,
      opacity: 0.85,
      duration: 3,
      repeat: -1,
      yoyo: true,
      ease: 'sine.inOut'
    });
  }

  // =========================================================
  // LUXURY FLOATING ROUNDED CUSTOM DROPDOWNS
  // =========================================================
  function initCustomDropdown(selectEl) {
    if (!selectEl || selectEl.dataset.customized === 'true') return;
    selectEl.dataset.customized = 'true';
    selectEl.style.display = 'none';

    const wrapper = document.createElement('div');
    wrapper.className = 'custom-select-wrapper';
    if (selectEl.id === 'liveMicDeviceSelect' || selectEl.id === 'liveVoiceLangSelect') {
      wrapper.classList.add('custom-select-right');
    }

    const trigger = document.createElement('div');
    trigger.className = 'custom-select-trigger';
    trigger.setAttribute('tabindex', '0');

    const labelSpan = document.createElement('span');
    labelSpan.className = 'custom-select-label';

    const chevron = document.createElement('span');
    chevron.className = 'custom-select-chevron';
    chevron.innerHTML = `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>`;

    trigger.appendChild(labelSpan);
    trigger.appendChild(chevron);

    const menu = document.createElement('div');
    menu.className = 'custom-select-menu';

    wrapper.appendChild(trigger);
    wrapper.appendChild(menu);

    selectEl.parentNode.insertBefore(wrapper, selectEl.nextSibling);

    function updateOptions() {
      menu.innerHTML = '';
      const selectedOption = selectEl.options[selectEl.selectedIndex] || selectEl.options[0];
      if (selectedOption) {
        labelSpan.textContent = selectedOption.textContent;
      }

      Array.from(selectEl.options).forEach((opt, idx) => {
        const item = document.createElement('div');
        item.className = 'custom-select-option' + (opt.selected ? ' selected' : '');
        item.textContent = opt.textContent;
        item.dataset.value = opt.value;

        item.addEventListener('click', (e) => {
          e.stopPropagation();
          selectEl.value = opt.value;
          selectEl.dispatchEvent(new Event('change', { bubbles: true }));
          wrapper.classList.remove('open');
          updateOptions();
        });

        menu.appendChild(item);
      });
    }

    trigger.addEventListener('click', (e) => {
      e.stopPropagation();
      const isOpen = wrapper.classList.contains('open');
      document.querySelectorAll('.custom-select-wrapper.open').forEach(w => {
        if (w !== wrapper) w.classList.remove('open');
      });
      wrapper.classList.toggle('open', !isOpen);
    });

    selectEl.addEventListener('change', () => {
      updateOptions();
    });

    wrapper._refresh = updateOptions;
    updateOptions();
  }

  function initAllCustomDropdowns() {
    document.querySelectorAll('select.aura-select, select.live-dropdown-pill, select.form-select').forEach(sel => {
      initCustomDropdown(sel);
    });
  }

  window.refreshCustomDropdown = function(selectEl) {
    if (!selectEl) return;
    if (selectEl.nextSibling && selectEl.nextSibling._refresh) {
      selectEl.nextSibling._refresh();
    } else {
      initCustomDropdown(selectEl);
    }
  };

  document.addEventListener('click', (e) => {
    if (!e.target.closest('.custom-select-wrapper')) {
      document.querySelectorAll('.custom-select-wrapper.open').forEach(w => w.classList.remove('open'));
    }
  });

  // =========================================================
  // SUNO AI - THEMED SVG LOADING SCREEN CONTROLLER
  // =========================================================
  function initSunoLoadingScreen() {
    const loadingOverlay = document.getElementById('sunoLoadingScreen');
    const typedTitle = document.getElementById('sunoLoadingTypedTitle');
    const cursor = document.getElementById('sunoLoadingCursor');

    if (!loadingOverlay) return;

    // Typewriter Word Animation ("SUNO AI")
    const fullText = "SUNO AI";
    let charIdx = 0;

    if (cursor) cursor.classList.add('active');

    // Wait 400ms after logo pops in before typing
    setTimeout(() => {
      const typeInterval = setInterval(() => {
        if (charIdx < fullText.length) {
          if (typedTitle) {
            typedTitle.textContent += fullText[charIdx];
          }
          charIdx++;
        } else {
          clearInterval(typeInterval);
          // Once typing completes, hold briefly then smoothly fade out
          setTimeout(() => {
            loadingOverlay.classList.add('fade-out');
            setTimeout(() => {
              loadingOverlay.remove();
            }, 650);
          }, 600);
        }
      }, 110);
    }, 450);
  }

  // Init
  renderHistory();
  renderCreditMonitor();
  initGsapAnimations();
  initAllCustomDropdowns();
  initSunoLoadingScreen();

  // Expose global hooks for Live Voice session continuity
  window.getLiveVoiceActiveMessages = function () {
    return activeMessages || [];
  };

  window.syncLiveVoiceUserMessage = function (text) {
    if (!text) return;
    const lastMsg = activeMessages[activeMessages.length - 1];
    if (lastMsg && lastMsg.role === 'user' && lastMsg.content === text) {
      return; // Deduplicate identical back-to-back turns
    }
    activeMessages.push({ role: 'user', content: text });
    renderMessage('user', text);
    saveSession();
  };

  window.syncLiveVoiceMessage = function (text) {
    if (!text) return;
    const lastMsg = activeMessages[activeMessages.length - 1];
    if (lastMsg && lastMsg.role === 'assistant' && lastMsg.content === text) {
      return; // Deduplicate identical back-to-back turns
    }
    const lastUserMsg = [...activeMessages].reverse().find(m => m.role === 'user');
    activeMessages.push({ role: 'assistant', content: text });
    renderMessage('assistant', text);
    saveSession();
    trackModelUsage('gemini', lastUserMsg ? lastUserMsg.content : '', text);
  };
});
